name: Build OXCE-XP

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  # build:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  # build-yaml-cpp:
  #   uses: ./.github/workflows/build-yaml-cpp.yml
  #   with:
  #     YAML_CPP_VERSION: 0.6.3

  build-mxe:
    uses: ./.github/workflows/build-yaml-cpp.yml
    with:
      MXE_VERSION: build-2022-01-13

  # build-mxe:
  #   runs-on: ubuntu-22.04

  #   env:
  #     MXE_VERSION: build-2022-01-13

  #   steps:
  #     - name: Cache MXE
  #       id: cache-mxe
  #       uses: actions/cache@v4
  #       with:
  #         path: /opt/mxe
  #         # key: ${{ runner.os }}-mxe-${{ hashFiles('**/mxe/**') }}
  #         key: ${{ runner.os }}-mxe-v${{ env.MXE_VERSION }}
  #         restore-keys: |
  #           ${{ runner.os }}-mxe-

  #     - name: Install MXE dependencies
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y autopoint gperf intltool libtool-bin lzip

  #     - name: Set up MXE environment (Windows XP support)
  #       if: steps.cache-mxe.outputs.cache-hit != 'true'
  #       run: |
  #         git clone https://github.com/mxe/mxe.git /opt/mxe
  #         cd /opt/mxe
  #         git checkout ${{ env.MXE_VERSION }}
  #         make MXE_TARGETS=i686-w64-mingw32.static JOBS=$(nproc) gcc cmake sdl sdl_gfx sdl_mixer sdl_image yaml-cpp

  #     - name: Save MXE cache
  #       if: steps.cache-mxe.outputs.cache-hit != 'true'
  #       uses: actions/cache@v4
  #       with:
  #         path: /opt/mxe
  #         key: ${{ runner.os }}-mxe-v${{ env.MXE_VERSION }}